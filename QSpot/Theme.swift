import SwiftUI

enum Theme {
    static let bg = Color(red: 0.018, green: 0.018, blue: 0.022)
    static let surface = Color(red: 0.065, green: 0.065, blue: 0.075)
    static let surface2 = Color(red: 0.095, green: 0.095, blue: 0.108)
    static let accent = Color(red: 0.83, green: 1.0, blue: 0.0)
    static let secondaryText = Color.white.opacity(0.58)
    static let divider = Color.white.opacity(0.07)
}
